config:
  target: 'http://localhost:3000'
  http:
    maxSockets: 15
  phases:
    - duration: 60
      arrivalRate: 10
  defaults:
    headers:
scenarios:
  - flow:
    - loop:
      - get:
          url: "/pictures/123456"
      count: 12
